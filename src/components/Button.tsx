import React from 'react';

const Button = () => {
   return (
      <button className="border px-9 py-3 rounded-3xl bg-gradient-to-r from-limeGreen from-50 to-brightCyan font-bold text-white hover:opacity-80 hover:duration-500">
         Request Invite
      </button>
   );
};

export default Button;
