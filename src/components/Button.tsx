import React from 'react';

const Button = () => {
   return (
      <button className="border-none px-9 py-3 rounded-3xl font-bold text-white hover:opacity-80 hover:duration-500 smallDevice:mx-auto smallDevice:block">
         Request Invite
      </button>
   );
};

export default Button;
